<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tally_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:clipToOutline="true"
    android:background="@drawable/widget_card_background">

    <ImageView
        android:id="@+id/tally_image"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scaleType="fitXY" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:paddingHorizontal="8dp"
        android:paddingBottom="8dp"
        android:orientation="horizontal"
        android:gravity="center">

        <TextView
            android:id="@+id/tally_minus"
            android:contentDescription="Decrease count"
            style="@style/Widget.Playbox.ControlLabel"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginHorizontal="2dp"
            android:text="−"
            android:textSize="@dimen/widget_text_title" />

        <TextView
            android:id="@+id/tally_reset"
            style="@style/Widget.Playbox.ControlLabel"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginHorizontal="2dp"
            android:text="RESET" />

        <TextView
            android:id="@+id/tally_plus"
            android:contentDescription="Increase count"
            style="@style/Widget.Playbox.PrimaryControl"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginHorizontal="2dp"
            android:text="+"
            android:textSize="@dimen/widget_text_title" />
    </LinearLayout>
</LinearLayout>
